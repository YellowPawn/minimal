{
  "name": "@yp.minimal/app",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "postclean": "sh svelte_init.sh",
    "dev": "vite --configLoader runner dev",
    "build": "rm -rf ./dist && vite --configLoader runner build"
  },
  "peerDependencies": {
    "@sveltejs/kit": "*",
    "vite": "*"
  },
  "dependencies": {
    "@sveltejs/kit": "2.55.0"
  },
  "devDependencies": {
    "@yp.minimal/components": "file:../components"
  }
}
